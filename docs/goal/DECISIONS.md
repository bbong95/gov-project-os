# DECISIONS

Record small Lean technical decisions that do not require a full ADR.

| Date | Milestone | Decision | Reason | Evidence/Test |
|---|---|---|---|---|
| 2026-08-24 | M00 | Use the Codex-bundled Node 24 and pnpm runtime for bootstrap commands instead of installing a duplicate system Node | The required major versions are already present and executable without machine-wide changes | Node 24.19.0 and pnpm 11.19.0 exited 0 |
| 2026-08-24 | M00 | Use Docker Desktop's WSL 2 Linux-container backend | Supabase local requires Docker; this is the official supported Windows path and adds no product infrastructure | WSL probe showed it is not yet installed |
| 2026-08-24 | M00 | Prepare Docker Desktop 4.87.0 x86_64 but do not execute it before H1 authorization | This is the current official Windows release and downloading/checking it is reversible while WSL installation is system-wide | Official checksum matched and Docker Inc. Authenticode signature was valid |
| 2026-08-24 | M00 | Supersede the bundled-Node-only bootstrap with the official portable Node.js 24.19.0 ZIP in the workspace | The portable distribution includes npm and needs no administrator or machine-wide installation | Official SHA-256 matched; Node 24.19.0, npm 11.17.0, and pnpm 11.19.0 exited 0 |
| 2026-08-24 | M00 | Install Docker Desktop in per-user mode without automatic license acceptance | The official mode is reversible and requires no administrator rights; legal acceptance and WSL system activation remain human-controlled | Installer log recorded success and Docker CLI 29.7.2 exited 0 |
| 2026-08-24 | M00 | Retain the WSL 2 backend instead of switching to Docker VMM Beta | WSL 2 is the declared baseline and default stable Windows backend; H1 approval removed the only reason to consider the Beta alternative | WSL 2.7.12.0 and Docker Server 29.7.2 verified |
| 2026-08-24 | M01 | Generate the official C3 app under `temp/` and copy only reviewed runtime files into the existing root | The root already contained Starter Kit V4.3 governance files and must not be overwritten | No target collisions; generated runtime committed as 08629b2 |
| 2026-08-24 | M01 | Select the OpenNext adapter explicitly instead of C3's recommended Vinext option | OpenNext is the declared Lean Architecture baseline and M01 gate | Generated package uses `@opennextjs/cloudflare` 1.20.2 and Wrangler 4.125.0 |
| 2026-08-24 | M01 | Allow build scripts only for `esbuild`, `sharp`, `unrs-resolver`, and `workerd` | These are the exact native packages required by the official generated lockfile; pnpm 11 blocks unapproved scripts | Root frozen install completed with each listed postinstall successful |
| 2026-08-24 | M01 | Initialize Git at the existing root and use a repository-local neutral automation identity | C3 correctly avoided initializing Git without identity; the long-running goal requires coherent commits without inventing a human identity | M00 commit 5ae3051 and M01 commit 08629b2 |
| 2026-08-24 | M01 | Verify OpenNext Workers runtime in a disposable official Node 24 Linux container after the native Windows bundle hit `EPERM` | OpenNext recommends WSL/Linux on Windows; the existing Docker WSL2 engine provides the runtime without adding product infrastructure or an unapproved Linux distribution | OpenNext build and Workers HTTP 200 passed; exact temporary container removed |
| 2026-08-24 | M02 | Keep V4.3 GOAL as the authoritative 60-file baseline and supplement only the missing M02-gate `.agents` and synthetic `fixtures` from V4.1 | V4.3 and V4.2 archives omit those two required directories; V4.1 provides the same lifecycle-aligned, manifest-hashed assets without colliding with V4.3 or generated runtime files | 60/60 V4.3 and 10/10 supplement files passed size/hash checks; runtime tracked diff exit 0 |
| 2026-08-24 | M03 | Pin the project-local Supabase CLI to 2.115.0 and the official browser/SSR libraries to 2.112.3/0.12.4 | Reproducible local setup must not depend on a machine-global CLI, and current official Next.js usage needs both browser and server helpers | Frozen install, CLI version, package imports, status and health probes passed |
| 2026-08-24 | M03 | Disable local Supabase Analytics and Storage Vector services | Neither service is required by the current vertical slice; disabling them avoids an unstable Vector container and preserves the no-dedicated-vector-infrastructure invariant | Final stack has 10 running containers, zero unstable, and zero analytics/vector containers |
| 2026-08-24 | M03 | Keep `.env.local` to the API URL and publishable key only, both Git-ignored | Browser code may use the publishable key, but service-role/OpenAI/database secrets must stay server-side and out of source | Two-key allowlist passed; forbidden markers and repository secret-pattern matches were zero |
| 2026-08-24 | M03 | Retain the official custom Docker bridge but document Docker Desktop's Windows port-proxy behavior | Docker Desktop still listens on all interfaces even with bridge host binding; changing machine firewall policy is outside the product slice | All Windows firewall profiles are ON with effective inbound blocking, and enabled Docker/Supabase-port inbound allow rules are zero |
| 2026-08-24 | M03 | Upgrade Next and eslint-config-next from 16.2.11 to 16.3.2 and opt out of generated agent-rule blocks | The earlier version carried validated high vulnerabilities; 16.3.2 fixes the affected transitive packages, while GOV Project OS already has authoritative root instructions | Audit is clean; build/dev/OpenNext preview pass; `AGENTS.md` hash is unchanged |
| 2026-08-24 | M04 | Use Vitest/jsdom/Testing Library for component behavior, Playwright Chromium plus axe-core for browser accessibility, and Supabase CLI pgTAP for the RLS runner | These are the declared baseline tools and exercise real rendered and database behavior without adding infrastructure | Unit 2/2, E2E 1/1, a11y 1/1, and pgTAP 1/1 passed |
| 2026-08-24 | M04 | Keep the Eval foundation to a generic deterministic check-summary contract | M09 owns product Eval metrics; M04 only needs a real non-placeholder runner that can report failed checks and exit non-zero through Vitest | Assertion RED then targeted GREEN 1/1 |
| 2026-08-24 | M04 | Replace the C3 legacy FlatCompat config with installed Next 16.3.2 flat configs and retain ESLint 9.39.5 temporarily | Next removed `next lint`; ESLint 10 is accepted by the top-level Next peer but rejected by its current import, JSX-a11y, and React plugins | ESLint 10 peer check failed on three plugins; ESLint 9 peer check and lint passed |
| 2026-08-24 | M04 | Supersede the M03 custom Docker bridge and use the standard Supabase CLI network | The custom bridge did not narrow the Docker Desktop Windows port proxy and made database tests non-reproducible; Windows firewall remains the effective local inbound boundary | Preserved-backup restart created `supabase_network_gov-project-os`; env values remained identical; machine-independent pgTAP passed |
| 2026-08-24 | M04 | Continue validating the package preview script in disposable official Node 24 Linux containers on this Windows host | Native OpenNext symlink creation remains unsupported on this Windows filesystem; Linux is the actual Workers build environment and no product infrastructure is added | Complete-source `pnpm preview` generated the Worker and returned HTTP 200; exact container removed |
| 2026-08-24 | M05 | Use database membership rows and operation-specific RLS as the authorization source of truth | User-editable or stale JWT metadata and application-only filters cannot prove tenant/project isolation for every Data API caller | Synthetic real-role pgTAP behavior was RED 14/20 without policies and GREEN 43/43 after minimal policies |
| 2026-08-24 | M05 | Grant `authenticated` table SELECT but only column-level UPDATE on `projects(name, updated_at)`; grant `anon` nothing | Explicit grants are required by current Supabase defaults and prevent accidental expansion beyond M05 project metadata editing | Catalog contract 23/23 and security/performance advisors reported no issues |
| 2026-08-24 | M05 | Manage Auth only through server-side Supabase SSR clients, Server Actions, and claims verification | Session cookies stay HTTP-only and no service-role/OpenAI credential enters browser code | Real login/project/logout E2E 3/3 and exact browser service-key scan hits 0 |
| 2026-08-24 | M05 | Create synthetic `.test` Auth, tenant, project, and membership rows at test runtime with the local service credential held only in test memory | Real customer data and privileged credentials must not become Git fixtures or logs | Assigned project visible, cross-tenant project absent, cleanup succeeds, tracked actual-secret files 0 |
| 2026-08-24 | M05 | Temporarily retain the deprecated `middleware.ts` Edge convention instead of Next 16 `proxy.ts` | Next 16 fixes `proxy.ts` to Node.js, while OpenNext Cloudflare 1.20.2 supports Edge Middleware but rejects Node Middleware | Linux OpenNext RED on Node proxy; manifest changed to edge true/node false; bundle and Workers HTTP 200 passed |
| 2026-08-24 | M05 | Run this small Playwright suite with one worker on the Windows workspace | Concurrent cold Turbopack requests intermittently returned a measured `/login` 404; a single worker removes the environment race without weakening assertions | Full E2E 3/3 and accessibility 2/2 passed on fresh servers |
| 2026-08-24 | M06 | Store immutable RFP originals in a private `rfp-originals` bucket and keep generic document metadata, explicit privacy classification, server SHA-256, and derived audit events in PostgreSQL | Original bytes must remain distinct from later parser/AI interpretation while retaining lifecycle-wide document reuse | Schema/isolation pgTAP 98/98 and exactly one upload audit event passed |
| 2026-08-24 | M06 | Authorize Storage with operation-specific project RLS, no UPDATE policy, no list policy, and owner-only deletion for unregistered compensation objects | Application checks alone cannot prevent overwrite or cross-project access; registered originals must be immutable | Real Storage E2E proved exact-byte read, cross-project/anonymous denial, zero list results, overwrite denial, registered-delete denial, and orphan cleanup |
| 2026-08-24 | M06 | Upload through a server Route Handler using the signed-in user client and a `StorageProvider`, capped at 6 MiB, with relative 303 locations | The browser must receive no privileged key; relative redirects remain cookie-safe behind host/proxy normalization | Browser RED exposed the absolute-host redirect defect; relative Location made editor/viewer E2E 2/2 GREEN |
| 2026-08-24 | M06 | Download untrusted originals as `application/octet-stream` with `private, no-store`, `nosniff`, and CR/LF-safe content disposition; do not parse or preview in M06 | Active rendering and invented interpretation are outside M06 and would weaken provenance/security | Exact downloaded bytes and safe filename behavior passed; no M07 parser or AI call exists |
