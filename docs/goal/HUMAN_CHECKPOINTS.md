# HUMAN_CHECKPOINTS

| ID | Status | Exact User Action | Why | Resume Condition | Resume Verification |
|---|---|---|---|---|---|
| H1 | COMPLETE | User authorized `wsl --install --no-distribution` and approved the UAC prompt | Docker Desktop required its WSL 2 backend | WSL 2 installed and Docker daemon responds | WSL 2.7.12.0; `docker info` exit 0 with Server 29.7.2 |
| STAGE_GATE_M05 | COMPLETE | User explicitly said `M05로 진행해` | The user required an explicit instruction before moving beyond M04 | M05 is authorized | M05 design/runbook inspected; schema contract RED is next |
| STAGE_GATE_M06 | COMPLETE | User explicitly said `M06로 진행해` | The user required an explicit instruction before moving beyond M05 | M06 was explicitly authorized | M06 private upload verification passed and evidence was recorded |
| M07_TRUST_BOUNDARY | COMPLETE | User explicitly approved ADR-0002 in this task | The approved no-service-role-endpoint design let an authenticated writer bypass the trusted parser and forge immutable SourceSpan evidence | ADR-0002 is Accepted and trusted persistence is committed | Direct authenticated RPC RED, actor mutation RED, final RLS 196/196, and both advisors clean in `bddfc87` |
| STAGE_GATE_M07 | COMPLETE | User explicitly said `M07로 진행해`, approved the design, approved the written M07 specification, and accepted ADR-0002 | The user required explicit authorization before moving beyond M06 and before changing the trusted persistence boundary | M07 Parser/SourceSpan is implemented and verified | RLS 196/196, E2E 7/7, axe 4/4, unit 25/25, Eval 1/1, build/Workers PASS, sealed security scan 0 findings |
| STAGE_GATE_M08 | COMPLETE | User explicitly said `M08로 진행해` and approved the architecture, data model, data flow, and verification design | The user required every milestone transition and architectural design section to stop at its Gate | M08 design sections are authorized | M07 evidence commit `185aa06` is clean; the four M08 design sections are consolidated in the written specification |
| M08_WRITTEN_SPEC | COMPLETE | User explicitly said `M08 작성 명세 승인` | The architectural brainstorming workflow required the committed specification to be reviewed before an implementation plan was written | Written specification approved | M08 TDD plan created without changing tests or production behavior |
| M08_IMPLEMENTATION_PLAN | COMPLETE | User explicitly said `M08 계획 승인, 인라인 실행` | The committed implementation plan required an execution choice before continuing | M08 plan and inline execution approved | Task 2 canonical-input RED resumes after the already completed Task 1 policy slice |
| STANDING_CONTINUE_2026_08_26 | COMPLETE (standing) | User said `지금 멈춘 단계에서 부터 끝까지 완벽하게 구현해` and repeatedly `계속 끝까지 진행해`; also directed KRDS design adoption (`웹디자인은 krds.go.kr 가이드대로 제작해`) | The user superseded the per-milestone stage-gate pause convention with a standing instruction to run the Goal end-to-end; KRDS adoption is an explicit product direction | All remaining milestones proceed without stage-gate pauses; H1–H10 human checkpoints still pause as defined | M08 completed with full matrix evidence; M09 started immediately after |
| KRDS_DESIGN_DIRECTIVE | COMPLETE | User directed the web UI to follow https://www.krds.go.kr | Product visual/UX baseline decision made by the user | KRDS applied via official `krds-uiux` assets vendored to `public/krds/` | E2E 11/11, a11y 8/8, independent review with the HIGH rem-scale regression fixed in `9e87f70` |
| H3_CLOUDFLARE_OAUTH | COMPLETE | User approved the `wrangler login` OAuth prompt in the browser on 2026-08-28; operator email and account ID are intentionally not recorded in Git | M22 deploy requires an authenticated Wrangler identity | Wrangler is authenticated | `wrangler whoami` exit 0; authorized identity, production account, and Workers write scopes verified locally |
| H4_SUPABASE_TOKEN | COMPLETE | User supplied a Supabase access token (2026-08-28); stored only in the CLI's persistent config, never committed or echoed | Hosted Supabase (M21) requires authenticated CLI access | `supabase projects list` exit 0 via stored credentials in a fresh shell without the token in env | Token not present in `.dev.vars`, Git, or command output; remote connection succeeded |
| H5_PRODUCTION_REGION | COMPLETE | User selected ap-northeast-2 (Seoul) on 2026-08-28 | Data residency / latency for a Korean public-sector product | Hosted project exists in the selected region | Project identity and region were verified without recording production credential values |
| H7_CONTRACT_BASELINE_APPROVAL | NOT_SATISFIED / EVIDENCE MISSING | Review and explicitly approve the Contract Baseline after its source lineage and conflicts are presented | Contract obligations cannot become the execution baseline without human approval | An authoritative H7 approval record exists and the immutable approved Contract Baseline Gate passes | Rerun the M14 contract baseline approval, immutability, provenance, and authorization checks |
| H8_TEMPLATE_MAPPING_APPROVAL | NOT_REACHED | Approve the first company/customer HWPX field mapping after M16 implementation and preview | Template mapping is a human-only factual/layout decision | M16 mapping UI and validation exist and an approval is recorded | Verify template hash/version, mapping coverage, unresolved required fields, validation, and preview |
| H9_MEETING_MINUTES_APPROVAL | NOT_REACHED | Approve meeting minutes before publishing Decision/Action/Issue/CustomerRequest records | AI Draft minutes cannot publish factual project records | M17 review flow exists and approved minutes are recorded | Verify no guessed people/owners/dates and no automatic Requirement Baseline mutation |
| H10_FINAL_ARTIFACT_ACCEPTANCE_CLOSEOUT | NOT_REACHED | Approve final artifact, acceptance, and closeout only after their validation evidence is presented | Final delivery and project closure require human judgment | M16/M19/M20 validation flows exist and approvals are recorded | Verify artifact validation/preview, acceptance evidence, open-item checks, and closeout record |
| M21_BLOCKED_H4_H5 | INPUTS RECEIVED / GATE NEEDS FRESH VERIFICATION | H4 token and H5 region inputs were received; no credential values are recorded | Hosted Supabase milestone still requires fresh consistency evidence before COMPLETE | Fresh hosted migration consistency and security checks pass | Rerun the M21 hosted Gate without echoing tokens, passwords, project refs, or full account identifiers |
| M22_DEPLOY_AUTH | BLOCKED (human-only token rotation) | Revoke the Cloudflare API token that was exposed in chat; create a new `Edit Cloudflare Workers` token scoped only to the production account; replace GitHub Actions values for `CLOUDFLARE_API_TOKEN` and `CLOUDFLARE_ACCOUNT_ID`. Never paste or record either value in chat, source, Git, screenshots, or logs. | The latest workflow received masked credentials, but Cloudflare rejected the token with codes `9109` and `10000`; a disclosed or invalid token must not be reused | The old token is revoked, replacement values are saved only in GitHub Actions, and no value is exposed | Rerun workflow run `33131163081`; require extraction, `.open-next/worker.js` verification, and Wrangler deploy to pass, then record only the deployment URL and run result |
